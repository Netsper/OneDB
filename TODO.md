# OneDB Sequential Delivery Todo

- [x] Frontend performance improvements (table browsing render cost + cell helper hot paths)
- [ ] CI pipeline setup (GitHub Actions: backend smoke + frontend tests + build)
- [ ] Critical E2E flow coverage (connection, browse/query, import/export)
- [ ] Backend observability hardening (`ONEDB_DEBUG` request timing/log path)
- [ ] Additional release size optimization pass
